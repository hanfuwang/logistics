<view class="content" style='width:100%;height:580px'>
<image src='../../images/餐厨线.svg'></image>
<!-- 
    <view class="">
     <image src="{{movie.images.large}}" mode="aspectFit"></image>
    </view>
    <view class="hr"></view>
    <view class="detail">
        <text>{{movie.text}}</text>
    </view>

    <view class="casts">
    <block wx:for="{{movie.casts}}">
        <image src="{{item.avatars.medium}}" mode="aspectFit"></image>
    </block>

     </view>
     <view class="hr"></view>
    <view class="detail">
        <text>{{movie.summary}}</text>
    </view>
<import src="../comment/loading"/>
<template is="loading" data="{{hidden}}"/> -->
</view>